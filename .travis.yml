# Travis CI Config - https://docs.travis-ci.com/user/languages/java/
jdk: oraclejdk10
language: java

# Improve Build Speed - https://dzone.com/articles/travis-ci-tutorial-java-projects
cache:
  directories:
  - $HOME/.m2

# Customizing The Build - https://docs.travis-ci.com/user/customizing-the-build/
before_install:
  - mvn -Ph2-database -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvn test -Ph2-database -B

# # Matrix Expansion - https://docs.travis-ci.com/user/build-stages/matrix-expansion/
 matrix:
   fast_finish: true
   include:
     - jdk: oraclejdk10
       env: DESC="All build with oraclejdk10"
     - jdk: openjdk10
       env: DESC="All build with openjdk10"

# Codecov - https://github.com/codecov/example-java
after_success:
  - bash <(curl -s https://codecov.io/bash)

# Notifications - https://docs.travis-ci.com/user/notifications/
notifications:
  email:
    recipients:
      - qThegamEp@gmail.com
    on_success: always # default: change
    on_failure: always # default: always
